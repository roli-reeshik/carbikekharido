// Self-contained, portable font configurations that require 0 external network requests
export const fontDisplay = {
  variable: "--font-display",
  className: "font-display",
};

export const fontBody = {
  variable: "--font-body",
  className: "font-sans",
};

export const fontMono = {
  variable: "--font-mono",
  className: "font-mono",
};
